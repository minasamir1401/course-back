# دليل الرفع على Dokploy (LMS Platform)

لقد تم تجهيز المشروع بالكامل ليكون جاهزاً للإنتاج. اتبع الخطوات التالية لرفع المشروع بنجاح على منصة **Dokploy**.

## 1. المتغيرات البيئية (Environment Variables)
عند إنشاء الخدمات في Dokploy، ستحتاج إلى إضافة المتغيرات التالية:

### للباك إند (Backend Service):
- `DATABASE_URL`: رابط قاعدة بيانات PostgreSQL الخاصة بك.
- `JWT_SECRET`: مفتاح عشوائي وفريد بطول 32 حرفاً على الأقل لتشفير الـ Tokens. أنشئه مثلاً عبر `openssl rand -base64 48` ولا تضعه في المستودع أو أي ملف JavaScript.
- `BACKEND_PORT`: اضبطه على `5000` (اختياري، الافتراضي هو 5000).
- `NODE_ENV`: اضبطه على `production`.

### للفرونت إند (Frontend Service):
- `NEXT_PUBLIC_API_URL`: الرابط الكامل للباك إند (مثال: `https://api.yourdomain.com/api`).
- `BACKEND_ORIGIN`: الرابط الأساسي للباك إند بدون `/api` (مثال: `https://api.yourdomain.com`).
- `FRONTEND_PORT`: اضبطه على `3000` (اختياري، الافتراضي هو 3000).
- `NODE_ENV`: اضبطه على `production`.

## 2. إعدادات Docker
المشروع مجهز بملفات `Dockerfile` في كل مجلد:
- **البناء (Build):** يتم تلقائياً عند الرفع.
- **قاعدة البيانات:** الباك إند يشغّل `prisma migrate deploy` فقط عندما تكون ملفات migrations موجودة. لا تستخدم `prisma db push --accept-data-loss` في الإنتاج لأنه قد يحذف جداول وبيانات موجودة.

## 3. المراقبة والصحة (Monitoring & Health)
- **رابط الصحة (Health Check):** الباك إند يوفر الآن مسار `/api/health` للتحقق من حالة التشغيل.
- **Docker Healthcheck:** تم دمج نظام تحقق تلقائي داخل الـ Dockerfile ليقوم Dokploy بمراقبة استقرار الخدمة.

## 4. الخطوات داخل Dokploy
1. **إنشاء مشروع جديد** في Dokploy.
2. **إضافة خدمة (Service)** من نوع "Docker Compose".
3. **ربط مستودع GitHub** الخاص بك.
4. **نسخ محتوى ملف `docker-compose.yml`** الموجود في المجلد الرئيسي للمشروع.
5. **إضافة المتغيرات البيئية** المذكورة أعلاه في قسم الـ Variables.
6. **اضغط على Deploy**.

### أمر التشغيل الآمن
اترك حقل **Start Command** فارغاً لكي يستخدم Dokploy أمر الـ Dockerfile:
`node src/scripts/start-production.js`.
إذا كانت المنصة تتطلب قيمة في الحقل، استخدم `npm start` فقط. لا تضف `prisma db push` أو `--accept-data-loss` إلى أمر التشغيل.

## ملاحظات هامة:
- **الصور:** يتم تخزين الصور حالياً في قاعدة البيانات (Base64)، لذا لا حاجة لإعداد Volumes للصور في الوقت الحالي.
- **التوافق:** تم التأكد من أن الفرونت إند يتخطى أخطاء الـ Lint والـ TypeScript البسيطة أثناء البناء لضمان عدم توقف الرفع.
- **الإغلاق الآمن:** تم إضافة نظام "Graceful Shutdown" للتأكد من إغلاق اتصالات قاعدة البيانات بشكل سليم عند إعادة التشغيل.

بالتوفيق! 🚀
